import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const UpperComponent = ({children}) => {
  return (
    <View style={{
        
    }}>
      {children} 
    </View>
  )
}

export default UpperComponent

const styles = StyleSheet.create({})